{
  "name": "sneaker-drop-server",
  "version": "1.0.0",
  "description": "Backend API for the real-time sneaker drop inventory system",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "seed": "node prisma/seed.js",
    "migrate": "prisma migrate dev",
    "generate": "prisma generate",
    "test:concurrency": "node scripts/concurrency-test.js"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
